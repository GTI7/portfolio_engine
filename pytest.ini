[pytest]
asyncio_mode = auto
# tests_ha/ is deliberately excluded from the default collection: it requires
# the full `homeassistant` package, installed only in the isolated
# .ha_test_venv (see TESTING.md and scripts/setup_ha_test_env.sh), not the
# main dev environment. Run it explicitly via that venv.
testpaths = tests tests_integration
